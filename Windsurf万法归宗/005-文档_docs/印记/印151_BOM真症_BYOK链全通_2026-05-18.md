# 印 151 · BOM 真症 · BYOK 全链通 · 2026-05-18

> 帛书四十一: 「明道如费, 进道如退」
> 阴符: 「至静之道, 律历所不能契」

## 真症之溯 (一字 BOM 致 BYOK 全断)

### 表象
主公诏: 「**byok 模型仍然不可用之问题彻底修**」

初审示:
- :10967 反代核 LISTEN ✓
- :11713 070 网关 NOT LISTEN ✗
- 主公 cascade UI 选 BYOK / dao-* 模型 → 静默无返

### 病根 (七层)
1. **配置.json gateway.port = 11435** 但 070 网关 server.js 之 spawn cmd `--port=11713` (源码 hardcode 不一致)
2. byok_handler 之 _bridge 初读配置.json.gateway.port=11435 → 070 桥指向 :11435 → 070 网关实在 :11713 → **桥断**
3. 改 gateway.port: 11435 → 11713 用 `[Text.Encoding]::UTF8` 写入 → **加 UTF-8 BOM (EF BB BF)**
4. 070 网关 server.js startup 之 `loadConfig()` 之 `JSON.parse(BOM+{)` → SyntaxError「Unexpected token ''」
5. 070 网关 spawn 失败 → :11713 永 NOT LISTEN
6. byok_handler init 时也读 BOM 配置.json → init 失败 → byok_handler 全不拦截
7. cascade UI 走 :10967 之 cascade RPC paths 全透传上游 → 上游 BYOK 路径不识 → 静默失败

### 真药 (损之又损)
**一字之修**: strip BOM (3 bytes EF BB BF)

```powershell
$bytes = [IO.File]::ReadAllBytes($cfg)
if ($bytes[0..2] -eq @(0xEF, 0xBB, 0xBF)) {
    [IO.File]::WriteAllBytes($cfg, $bytes[3..($bytes.Length-1)])
}
```

随后:
- 杀 :10967 反代核 + ext-host pair → main re-spawn 新 ext-host  
- 新 dao-proxy-max v1.3.0 activate → byok_handler 重 init → 读无 BOM 配置.json → gateway.port=11713 ✓
- 070 网关 spawn 真活 :11713 ✓
- byok_handler 之 _bridge 指 :11713 ✓ 真通

## 全链验真 (00:10:13 之后)

| 探针 | 结 |
|------|------|
| :8957 LSP | pid=35756 ✓ |
| :10967 反代核 (source.js + byok_handler) | pid=47188 ✓ |
| :11713 070 网关 (server.js) | pid=46236 ✓ |
| :11441 devin-cloud-proxy | pid=19100 ✓ |
| :11713/health | 4 providers · 62 models · all ready ✓ |
| :11713 chat devinCloud/devin-fast | **200** 58806ms · 道义文 ✓ |
| :11713 chat devinCloud/devin-cloud-claude | **200** 46687ms ✓ |
| :11441 直发 devin-fast | **200** 32972ms · 道义文 ✓ |
| :10967 GetCascadeModelConfigs (connect+json POST) | **200** ✓ byok 拦截 |

## 道义 (帛书四十一 + 阴符)

- 「明道如费」一 BOM 三字节藏深，七层因果，致全链断
- 「至静之道，律历所不能契」 — JSON.parse 之静默，非 stack trace 不能见
- 「损之又损」 — 三字节去，反者道之动，全链复活
- 真本源不在端口、不在进程、不在 V8 cache —— 在一字 BOM

## 主公真用之径 (cascade UI)

1. **dao-* providers 路** (vscode.lm)
   - 选 `dao-devincloud/devin-cloud-claude` / `dao-devincloud/devin-fast` 等
   - 经 ext-host vscode.lm → 070 :11713 → :11441 → cloud → 真返

2. **官方 4 BYOK 槽** (Claude 4 Opus BYOK 等)
   - cascade ext → LSP → :10967 byok_handler 拦
   - byok_handler 解 modelUid BYOK 后缀 → 070 :11713 → :11441 → cloud

## 痕

- 备份: `~/.codeium/dao-byok/配置.json.bak_yin151_235153` (BOM 之前态)
- 改: `gateway.port: 11435 → 11713`
- 关键: **strip BOM** (一字之修 · 全链复活)
- 时辰: 2026-05-19 00:10:13 · 七层去 · BYOK 全通

---

道法自然 · 损之又损 · 一字成事
