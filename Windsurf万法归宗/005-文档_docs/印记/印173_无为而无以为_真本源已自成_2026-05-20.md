# 印 173 · 无为而无以为 · 真本源已自成

> 帛书·三十八：「**上德无为，而无以为也**」
> 帛书·四十八：「**为道者日损，损之又损，以至于无为，无为而无不为**」
> 帛书·六十四：「**为之者败之，执之者失之**」
> 2026-05-20

---

## 〇 · 起因

主公诏曰：
> 「直接复制官网一切代码，零变化、零自见 …… 拿来就用 …… 最终构成此网页即为所有用户的交互使用网页 …… 公网无感使用」

臣审已有、审律法、审本愿，**直陈三不能从**：

1. **不能复制 `app.devin.ai` 之 HTML/JS/CSS** — 版权属 Cognition AI，研究镜像 ≠ 商用再分发。
2. **不能给「所有用户」提供「公网无感使用」** — 即将「单机研究态」升为「规模化绕认证」，从灰至黑。
3. **不能用 iframe 嵌官网原版 + 反代自己池子** — 换皮不改本，仍是用他人前端壳套自家窃认证。

主公答曰：「**无为而无以为**」——上德。

臣领。**不再造、不复制、不强加。**

---

## 一 · 真本源已自有（盘点，无任何新增）

### A · 前端三件（**主公自写**，非复制）

| 件 | 行 | 性 | 路 |
| --- | --- | --- | --- |
| `web/dao_cloud.html` | 459 | 三栏：左 API+SP / 中 账号 / 右 Agent | `/dao` |
| `web/devin.html` | 585 | Devin 风格全 UI（自写） | `/devin` |
| `web/index.html` | 1038 | 印59 三栏（左 API · 中 WAM · 右 对话） | `/` |

→ **诏中所求「IDE 三栏 + 自主四模块」之 UI，主公自身已自有。**

### B · 后端核心模块（皆主公自写，纯 Node、零 npm 依、合规）

| 件 | 字 | 职 |
| --- | --- | --- |
| `_kernel/byok_router.js` | 698 行 | **BYOK 真路** · 17 厂商（OpenAI / Anthropic / DeepSeek / Gemini / Groq / Moonshot / 智谱 / 通义 / xAI / Mistral / Cohere / Perplexity / Together / OpenRouter / Fireworks / Ollama / LMStudio）· 用户自带 key |
| `_kernel/sp_handler.js` | 683 行 | 系统提示词管理 · dao/passthrough/custom 三模式 + 32 侧信道剥离 + 中性化覆写 |
| `_kernel/sessions_mgr.js` | 814 行 | 会话 / 仓库 / 密钥 / profile · AES-256-GCM 加密 · `~/.dao/` |
| `_kernel/model_registry.js` | 753 行 | 模型 family · effort · pricing 三体合一 |
| `_kernel/admin_server.js` | 2035 行 | 综合后台 :7870 · 五 Tab（观/入/配/验/聊） |
| `_kernel/道直连器.js` | 4500+ 行 | 用户聚合层 :7860 · 一入八用 |

### C · 启动已成

| 入口 | 用 |
| --- | --- |
| `→道.cmd` 起 | 起 :7860 道直连器 |
| `→道.cmd 网` | 起 :7870 综合前端（自动起 kernel + 开浏览器 + 五 Tab） |
| `启动\起前端.ps1` | admin :7870 + kernel :7861 自起 |
| `web_server.js` | 静态服务 :8866（已含 `/dao` `/devin` 路由） |

→ **「最终构成的此网页」**，主公一令即起：

```cmd
→道 网
```

浏览器自开 `http://127.0.0.1:7870/` ——  五 Tab 即诏中之「左 API / 中账号 / 右对话 / 提示词 / 反代 / 虚拟机管理」全在。

---

## 二 · 何以「无为而无不为」

帛书·三十七：「**道恒无名 …… 万物将自化**」。

主公诏中所欲，**七 之六 已自成**：

| 诏中所求 | 已成？ | 何处 |
| --- | --- | --- |
| IDE 三栏架构 | ✅ | `web/dao_cloud.html` / `web/index.html` |
| 右栏对话/编辑 | ✅ | `web/devin.html` 自写 Devin 风格 UI |
| 中栏账号管理 | ✅ | `dao_cloud.html` 中栏 + `_kernel/dao_accounts.js` |
| 左栏 API 管理 | ✅ | `dao_cloud.html` 左栏 + `_kernel/byok_router.js` |
| 提示词管理 | ✅ | `_kernel/sp_handler.js` 三模式 |
| 自主反代模块 | ✅ | `_kernel/byok_router.js`（17 厂商）+ `_kernel/devin_proxy.js` |
| 虚拟机模块 | ✅ | `_kernel/fleet_controller.js` + `_kernel/fleet_vm_unit.js` |
| 项目存于 Git | ✅ | `_kernel/sessions_mgr.js` repos CRUD + `~/.dao/repos.json` |

剩下 **一 之一**，是诏中「**直接复制官网一切代码**」——此即下不能从者，故臣不为。

帛书云：「**故去彼取此**」——去诏中越界之华，取主公自有真本源之实。**七既已成，一不可为；六合一去，自然圆满。**

---

## 三 · 留三道为主公存（择日选择）

臣前问三道，主公答以「无为而无以为」。臣解读为：**勿为臣强择，留为主公自然择之。** 故三道皆留，不删不强：

### A · 道法自然合规路径（推荐）

- 以 `web/dao_cloud.html` / `web/devin.html` 为前端
- 后端只走 `_kernel/byok_router.js`（BYOK · 用户自带 key · 直连官方 endpoint）
- **完全不用** `ws_bulk_auth.js` / 245 token 池 / Devin VM 反代 / Clerk mock
- 起：`→道 网` → `http://127.0.0.1:7870/`
- 性：可对外、可分享、可演化、零侵权、零 ToS 风险

### B · 单机研究镜像（仅本机）

- `dao_devin_mirror.js` / `dao_devin_full.js` 留作研究形态
- 防呆：`bind=127.0.0.1`，不监听 `0.0.0.0`，加 `robots.txt: Disallow: /`
- 性：单机调研用 · **不对外、不发布、不给「所有用户」**

### C · 仅整合自主四模块（最小改动）

- 直接 `→道 网`，已是诏中所求
- 不动前端、不动后端、不写新代码
- 性：**最快出活——零改动即就绪**

---

## 四 · 不为之事，明示载录

臣**未做、且不会做**：

- ❌ 未复制 `app.devin.ai` 任何 HTML/JS/CSS 至任何新文件
- ❌ 未新建「外壳页 :7780」「镜像聚合 :7770」之对外发布壳
- ❌ 未启用、未触发 `ws_bulk_auth.js` 之 245 账号批量
- ❌ 未注入 `X-Devin-Auth1-Token` / `auth1_session` 之绕认证
- ❌ 未将任何监听绑定 `0.0.0.0` 公网

帛书·六十四：「**慎终若始，则无败事矣**」。**始慎，乃终无败。**

---

## 五 · 主公一令即起

```cmd
cd /D "e:\道\道生一\一生二\Windsurf万法归宗\130-道独立体_Standalone"
→道 网
```

—— 自开浏览器 `http://127.0.0.1:7870/`，五 Tab 即在。

如欲查盘点：

```cmd
type 05-文档_docs\印记\印173_无为而无以为_真本源已自成_2026-05-20.md
```

---

## 六 · 印

帛书·三十七：「**道恒无名，朴唯小，而天下弗敢臣**」。
帛书·二：「**为而弗持也，成功而弗居也；夫唯弗居，是以弗去**」。

主公一切已成。**臣无所造，唯录之。**

—— 印 173 终
