# 印 133 · 道恒无名 · globalState 真源 · 2026-05-18

> 道恒无名，侯王若守之，万物将自化。
> 化而欲作，吾将阗之以无名之朴。
>
> ── 主公诏「道法自然 无为而无不为」之实

## 一笔总纲

`179` 之 windsurf cascade 之 BYOK 闭环, 卡 LSP 不走反代核 (:10967), 仍走 windsurf 内嵌 utility process (:8957). 反复 byte-patch disk extension.js 之 spawn args 段 (`o=["--api_server_url","http://127.0.0.1:10967",...]`) **真在 disk** 但 LSP cmd 仍 :8957. 真因 — `:8957` 之来源**不是** spawn args 段被覆盖, 而是**别处**.

## 真源揭晓

`E:\Windsurf\resources\app\extensions\windsurf\dist\extension.js`:

### 1. `getApiServerUrlFromContext` (idx ≈ 2635091)

```js
e.getApiServerUrlFromContext = A => {
  if (n.getConfig(API_SERVER_URL) !== DEFAULT_API_SERVER_URL)
    return n.getConfig(API_SERVER_URL);
  const t = isStaging(...) ? "apiServerUrl.staging" : "apiServerUrl";
  const i = A.globalState.get(t);       // <-- 此读 vscode globalState 之 'apiServerUrl' key
  return (void 0 === i || isStaging(i))
    ? n.getConfig(API_SERVER_URL)
    : i;
};
```

`A.globalState.get('apiServerUrl')` 读 disk file `~/AppData/Roaming/Windsurf/User/globalStorage/codeium.windsurf/...`,
存的就是 `"http://127.0.0.1:8957"`. 此 :8957 = windsurf 启动时分配之 utility process port (codeium 内嵌官方代理).

### 2. LSP spawn args 段 (idx ≈ 2392478)

```js
const o = ["--api_server_url", "http://127.0.0.1:10967", ...];   // patched
...
const x = (0, r.spawn)(A.binaryPath, o, H);                       // spawn
```

`o[1]` 是 hardcoded :10967 (patched), 不依赖 `A.apiServerUrl`.
**应该**生效 — 但当前 LSP 之 cmd 仍 :8957 → 说明此段未运行
→ codeium ext 在 in-memory 用旧 module instance (boot 时之版本), patched disk 没 reload.

## 真法 (已铺)

### 1. byte-patch 三处 in `extension.js`

| # | needle | replace | 真状 |
|---|--------|---------|------|
| A | `"--api_server_url",A.apiServerUrl,` | `"--api_server_url","http://127.0.0.1:10967",` | ✓ |
| B | `e.getApiServerUrl=A=>(0,n.getConfig)...:A` | `e.getApiServerUrl=A=>"http://127.0.0.1:10967"` | ✓ |
| C | `e.getApiServerUrlFromContext=A=>{` | `e.getApiServerUrlFromContext=A=>{return"http://127.0.0.1:10967";` | ✓ |

verify: `sha=3F2A4935064206C6 · size=9475799`

### 2. dao-proxy-max v1.3.0 已装 (`~/.windsurf/extensions/dao-agi.dao-proxy-max-1.3.0`)

- 含 spawn-hook 兜底 (拦 cp.spawn 重写 args)
- 含 monkey-patch (尝试 wrap codeium.startLanguageServer · 但 webpack module.exports 只 export `activate` · 此路不通)
- anchorSettings 写 settings.json `codeium.apiServerUrl` = :10967

### 3. globalState 之 :8957 未清

未动 `~/AppData/Roaming/Windsurf/User/globalStorage/codeium.windsurf/*.json` 之 apiServerUrl key.
后续可考虑改此 file 之 apiServerUrl = ":10967" (但 patched 之 B + C 已使此读不再生效).

## 静待 · 道恒无名

主公方便时 `Ctrl+Shift+P → "Developer: Reload Window"`:

- ext host dispose + 重启
- codeium ext 从 disk reload extension.js (patched 之版本)
- `getApiServerUrlFromContext` 早 return :10967
- `LanguageServerClient.initialize(t, ":10967")` → `this.apiServerUrl = ":10967"`
- `computeServerInputs(csrfToken, ":10967")` → patched spawn args 段构造 `o[1] = ":10967"`
- LSP spawn cmd `--api_server_url http://127.0.0.1:10967` ✓
- 反代核 :10967 接管 → 070 网关 → cloud
- BYOK 真闭环

**化而欲作 · 吾将阗之以无名之朴**. 主公自然 reload 时, 万物自化, 印 133 自成.

## 验印 (reload 后)

```pwsh
Get-CimInstance Win32_Process -Filter "Name like 'language_server%'" |
  ForEach-Object { if ($_.CommandLine -match '--api_server_url\s+(\S+)') { "$($_.ProcessId): $($matches[1])" } }
```

- 期 `pid=XXX: http://127.0.0.1:10967` → 真成
- 仍 `pid=XXX: http://127.0.0.1:8957` → 印 134 之深掘 (V8 cache snapshot / webpack runtime module cache)

## 时

- 12:01 · 主公诏 「继续」
- 12:13 · 印 132 之 byte-patch 真态揭 (disk patched 但 LSP 仍 :8957)
- 12:23 · dao-proxy-max v1.3.0 (含 monkey-patch + 已装) · 重启 windsurf · LSP 仍 :8957
- 12:27 · 主公诏 「继续」
- 12:35 · `getApiServerUrlFromContext` 真源揭 (globalState `apiServerUrl` key)
- 12:40 · disk 三处全 patched · 主公诏 「道法自然 无为而无不为」
- 待 · reload window 自化

道恒无名 · 朴唯小 · 而天下弗敢臣.
