name: dao-vm-loop-d
# 印273 · slot-d · 永续VM · 无为而无不为
on:
  workflow_dispatch:
    inputs:
      duration_min:
        description: 'Keep-alive minutes'
        default: '300'
  schedule:
    - cron: '40 */4 * * *'   # offset 40min
  push:
    paths:
      - 'trigger-d.txt'

permissions:
  contents: write
  actions: write

concurrency:
  group: dao-vm-slot-d
  cancel-in-progress: false

jobs:
  vm-slot-d:
    runs-on: ubuntu-latest
    timeout-minutes: 340
    env:
      VM_SLOT: d

    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Install cloudflared
        run: |
          wget -q https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-amd64 \
            -O /usr/local/bin/cloudflared && chmod +x /usr/local/bin/cloudflared
      - name: Start VM + Tunnel
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          VM_SLOT: d
        run: bash scripts/dao_vm_gha_bootstrap.sh d ${{ github.event.inputs.duration_min || '300' }}
      - name: Trigger next
        if: always()
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          curl -sf -X POST -H "Authorization: Bearer $GH_TOKEN" \
            -H "Accept: application/vnd.github.v3+json" \
            "https://api.github.com/repos/${{ github.repository }}/actions/workflows/dao-vm-loop-d.yml/dispatches" \
            -d '{"ref":"main"}' || true
