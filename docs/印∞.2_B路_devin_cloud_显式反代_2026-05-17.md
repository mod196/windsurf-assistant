# 印 ∞.2 · B 路 · /dc/\* 显式 devin cloud 反代 · 双反代之实

**立**: 2026-05-17
**承**: 印 ∞ 「★ 对照 tab + A/B 双路探活」
**之核**: 治本源真断 · UI 期 B 路 vs daemon 未实之**契约错位**

---

## 主公本源诏 (2026-05-17 5:33PM 截图)

> **「devin vm 虚拟机资源双反代 windsurf + devin cloud 底层模型到公网无感使用 api」**
>
> 「反代链路一气化三清 · 一为 devin cloud vm 虚拟机无限制可并发 ·
> 一 windsurf 账号一 vm 运行双反代+隔离提示词 · 二为用户端 github 账号统一公...」

主公诏「**双反代**」 — 意指**两路独立显式之反代**:

| 路 | 路径 | 引擎 | 用 |
|----|------|------|---|
| **A 路** | `/v1/*` | 智能分流 (含 ws/cascade → ws · 否则 → devin) | 主 mainstream · OpenAI 客户端标准 |
| **B 路** | `/dc/*` | **显式 devin cloud (跳分流)** | 调试 · 绕道 · 强测 devin cloud |

---

## 真断真显 (印 ∞.2 之前)

cascade 5:33PM 后深入解构 · 发**重大本源真断**:

- **dao_proxy.js** 仅实 `/v1/*` (智能分流) + `/windsurf/*` (印 104 ws 显式)
- **缺 `/dc/*`** B 路 (devin cloud 显式) 完全未实现
- **web/dao_app.js** L4459 `probeABRoutes` 期 vmUrl + `/dc/v1/models` + `/dc/health`
- 故 UI **B 路探活永 404 红** — UI 与后端**契约错位**!

---

## 印 ∞.2 之治 (柔法 · 弱者道之用)

### ① handleOpenAI 加 forceEngine 参 (跳智能分流)

```js
async function handleOpenAI(req, res, forceEngine) {
  // ...
  // 印 ∞.2 · forceEngine="devin" 时跳此判 · 显式走 devin cloud
  if (forceEngine !== "devin" && isWindsurfModel(model)) {
    return handleWindsurfChat(...);
  }
  // ... 继续走 devin
}
```

### ② 立 handleDcHealth + handleDcModels

```js
function handleDcHealth(req, res) {
  sendJson(res, 200, {
    ok: true, route: "B", engine: "devin-cloud",
    seal: SEAL, version: VERSION, upstream: CFG.wssUrl,
    pool: { total, cursor, ok_count },
    sp: { strategy, customSpLen },
    note: "B 路 · 显式 devin cloud · 跳智能分流 · 印 ∞.2",
  });
}
function handleDcModels(req, res) {
  // 返同 MODELS · owned_by="devin-cloud" · x_dao.route="B"
}
```

### ③ 主路由加 /dc/\* 5 路

```js
if (method === "GET" && p === "/dc/health") return handleDcHealth(req, res);
if (method === "GET" && p === "/dc/v1/models") return handleDcModels(req, res);
if (method === "POST" && p === "/dc/v1/chat/completions")
  return handleOpenAI(req, res, "devin");   // 强 devin
if (method === "POST" && p === "/dc/v1/messages")
  return handleAnthropic(req, res);         // 本来就走 devin
if (method === "POST" && p.startsWith("/dc/v1beta/models/"))
  return handleGemini(req, res, p.slice(3)); // 剥 /dc 前缀
```

### ④ 404 hint 含 /dc/\* 5 路 · SEAL 升至 印 ∞.2

```
SEAL = "印 122/125/130/∞.2 · ... · /dc/* B 路双反代 · 反者道之动"
VERSION = "0.4.3"
```

---

## 守门 (_seal_inf2_dc_routes_smoke · 22/22 全过)

### §1 · 静守 (14 项)

- SEAL 含 印 ∞.2 标
- VERSION ≥ 0.4.3
- function handleDcHealth · handleDcModels 真存
- handleOpenAI 加 forceEngine 参 · 跳 isWindsurfModel 智能分流
- 主路由 5 路 (GET /dc/health · /dc/v1/models · POST /dc/v1/chat/completions · /dc/v1/messages · /dc/v1beta/models/)
- /dc/v1/chat/completions → handleOpenAI(req,res,"devin")
- /dc/v1beta/... → handleGemini(req,res,p.slice(3)) 剥前缀
- 404 hint 含 /dc/\* 5 路

### §2 · 动守 (8 项 · 真起 daemon · 真路验)

- daemon 起 · /health=200
- **/dc/health** → 200 · route=B · engine=devin-cloud
- **/dc/v1/models** → 200 · 16 models · owned_by=devin-cloud
- **/dc/v1/chat/completions** cascade → forceEngine=devin → 503 no_token (跳分流真活)
- **/dc/v1/messages** → 503 no_token
- **/dc/v1beta/models/devin-cloud:generateContent** → 503 no_token
- 404 hint 含 /dc/\* 5 路 + 印 ∞.2 标
- **/v1/chat/completions cascade** → A 路 503 (与 B 路对比真 · 物无非彼物无非是)

---

## 帛书引

| 章 | 引 | 应 |
|----|-----|----|
| 二 | 「美与恶 之相生」 | A 路智能分流 + B 路显式同一 dao_proxy · 物无非彼物无非是 |
| 廿二 | 「圣人执一」 | 一 handleOpenAI 共两路 · forceEngine 一参分流 |
| 四十 | 「反者道之动」 | UI 期不实 → 反向治 · daemon 实 B 路 · 契约同 |
| 四十八 | 「为道日损」 | 不立新 handler · 复用 handleOpenAI/Anthropic/Gemini · 仅加 forceEngine + 5 路注 |
| 七十八 | 「天下莫柔弱于水 · 弱者道之用」 | forceEngine 一字之柔 · 治本源真断 |

---

## 真断治后状

| 视角 | 印 ∞ 之前 | 印 ∞ (UI 立 probeABRoutes) | **印 ∞.2 (本印 · daemon 立 B 路)** |
|------|----------|----------------------------|--------------------------------------|
| dao_proxy | 仅 /v1/\* + /windsurf/\* | 同 (UI 期未实) | **+ /dc/\* 5 路 (B 路)** |
| web UI A/B 探活 | 无 | A=/v1/models ✓ · B=/dc/v1/models ✗ 404 | A=/v1/models ✓ · **B=/dc/v1/models ✓** |
| 双反代真实 | 半 (仅 A 路) | 半 (UI 显但 B 探永失) | **全 (UI ↔ daemon 契约同)** |

---

## 接续

- 印 ∞.3+ 之候: 真接 windsurf API key 之 ws 池 · 真打 B 路 cascade 模型 (现在仅 503 no_token)
- README + INDEX_GUIZONG.md 同步加 /dc/\* 章 (本印自补)
- web/dao_app.js 之 A 路注释「/v1/\* = Windsurf codeium」实际 inaccurate · A 路是「智能分流」 (待印 ∞.3 微正)

---

**印**: ∞.2
**章**: 二 + 廿二 + 四十 + 四十八 + 七十八 (五帛书章合一)
**核**: 双反代真实 · A/B 契约同 · 弱者道之用
**触**: dao_proxy.js (+72) · tests/_seal_inf2_dc_routes_smoke.cjs (+250 新) · tests/run_all.cjs (+1) · tests/_seal128_yiqi_sanqing_smoke.cjs (+1)
**验**: 守门 27/27 全套 + 印 ∞.2 22/22 专项 · 道法自然 · 无为而无不为
